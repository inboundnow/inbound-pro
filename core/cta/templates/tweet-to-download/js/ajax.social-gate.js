function fb_like_to_download_event(){	/***record impressions***/	var conversion_data = {		action: 'wp_cta_record_conversion',		current_url: wp_cta_settings.current_url	};	jQuery.post(wp_cta_settings.ajaxurl, conversion_data, function(response) {		//alert('here!');		if (response!=false)		{			//alert('Got this from the server: ' + response + myajax.ajaxurl);			//form.submit();			return true;		}	});}